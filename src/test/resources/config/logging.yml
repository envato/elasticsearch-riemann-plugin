rootLogger: INFO, console

logger:
  service.riemann: DEBUG
  plugin.riemann.RiemannPlugin: DEBUG

appender:
  console:
    type: console
    layout:
      type: consolePattern
      conversionPattern: "[%d{ISO8601}][%-5p][%-25c] %m%n"
